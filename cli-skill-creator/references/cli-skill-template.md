---
name: <tool>-cli
description: >
  <One or two sentences on what the CLI does.> Use whenever the user mentions
  <tool>, <config files like tool.toml>, or asks to <list the main verbs:
  install / sync / create / run / inspect ...>. Also use whenever <a marker file
  or config> is present and the user is doing <domain> work.
---

# <Tool> CLI

<One-paragraph description: what the CLI is, what the Python package is named,
what the command is, and what this skill helps an agent do on the user's
behalf.>

## When to use

Use this skill when the user wants to:

- <verb + object, mirroring a CLI command — e.g. "install a skill (`<tool> add ...`)">
- <...one bullet per common task...>

Do NOT use this skill for:

- <out-of-scope task, and where to go instead>

## Prerequisites

Before running anything, verify the CLI is installed:

```bash
<tool> --version
```

If missing, the standard install is `uv tool install <tool>` (or `pip install
<tool>`). **Do not install it without checking with the user first** — they may
have a pinned version or a different install method.

## Mental model — read first

<The 3-6 facts an agent must hold in its head to use the CLI correctly without
reading the source. Define the core nouns, the file(s) the CLI reads/writes,
any local-vs-global or manifest-vs-lockfile distinctions, and any "X only does
Y, use Z for W" gotchas. This section is what makes the skill an operating
manual rather than a command dump.>

## Workflows

### 1. <Task name, e.g. "Set up a new repo">

<Numbered steps. Show the exact commands. Note flags that matter and the common
mistakes.>

```bash
<tool> <command> ...
```

### 2. <Next task>

...

## Boundaries

- **<Never-do, with the reason>** — e.g. "Never edit `<tool>.lock` by hand; it's
  regenerated by `<tool> sync`."
- **<Confirm-before, with the reason>** — e.g. "Confirm before `--overwrite` on a
  file the user has been editing."
- **Don't push or `git tag`** as part of these workflows — commit locally and let
  the user (or the `<tool>-release` skill) handle releases.

## Troubleshooting pointers

- "<common error>" → <cause and fix>.
- ...

## Keeping this skill honest

This skill's command list must match `<tool> --help`. When the CLI surface
changes, update this file in the same PR. <If the skill is bundled inside the
package too, name the second copy here and note both must change together.>

## References (optional)

- [<topic>.md](references/<topic>.md) — <what it covers>
